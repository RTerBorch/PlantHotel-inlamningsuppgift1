package ObjectOrienteradKurs.Sprint1.Inlämningsuppgifter.Greenest;

import ObjectOrienteradKurs.Sprint1.Inlämningsuppgifter.Greenest.Program.Program;

public class GreenestMain {

    public static void main(String[] args) {

        Program.växtProgram();

/*
        // Skapar nya växter, varje växt har en egen klass
        Växter v1 = new Kaktus(0.2, "Igge");
        Växter v2 = new Palm(5, "Laura");
        Växter v3 = new KöttätandeVäxt(0.7, "Meatloaf");
        Växter v4 = new Palm(1, "Putte");
        // Lägger in i Arraylist
        List<Växter> plantList = Arrays.asList(v1,v2,v3,v4);


        //String waterWhatPlant = JOptionPane.showInputDialog(null,"Vilken växt vill du vattna? ");
       // waterWhatPlant = waterWhatPlant.trim(); // Tar bort eventuella mellanlag.
       String waterThisPlant = Message.waterWhatPlant();
       boolean hittat = false;
        for (Växter element : plantList) { // Om waterWhatPlant innehåller ett namn som finns i arraylist, returnera vattnaMedVad.
            if (element.getNamn().toLowerCase().equals(waterThisPlant)) {
                element.vattnaMedVad();
                hittat=true;
            }
        }
        if (!hittat){
            System.out.println("Växten du angav fanns inte i listan");
        }

 */


    }
}
